let altura = 173;
let alturaMetro = 1.73;
let peso = 63.5;

let alturaRedond = alturaMetro.toPrecision(2);
console.log(alturaRedond);

let pesoRedond = (peso.toPrecision(2)) - 1;
console.log(pesoRedond)

let maxValor = (Number.MAX_VALUE + 1) 

console.log(maxValor === Number.MAX_VALUE);
/// SETS o CONJUNTOS: tipos de objetos, que son como una lista pero se comportan diferente
//La diferencia entre un array y un set es que, éste no nos va a permitir almacenar valores repetidos.

const arr = [1, 2, 3, 4, 5];

const miSet = new Set(arr);
console.log(miSet)